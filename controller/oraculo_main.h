#ifndef oraculo_main_h // criando o cabeçalho do arquivo oraculo_main.h
#define oraculo_main_h // definindo o cabeçalho do arquivo oraculo_main.h

void limpar_entrada(void);
void iniciar_jogo(char nome[]);
void mostrar_instrucoes(void);

#endif // oraculo_main_h

 